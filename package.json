{
  "name": "pinus-platform",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.28.1",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "db:generate": "pnpm --dir apps/api prisma generate",
    "db:migrate": "pnpm --dir apps/api prisma migrate dev",
    "db:seed": "pnpm --dir apps/api prisma db seed"
  }
}
